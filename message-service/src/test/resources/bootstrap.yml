
# Disable the Consul registration for the tests

spring:
  application:
    name: message-service
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
